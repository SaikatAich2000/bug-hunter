# Development and CI dependencies. Not shipped in the runtime image; the
# production Dockerfile installs requirements.txt only.
#
#     pip install -r requirements.txt -r requirements-dev.txt

# Tests
pytest>=8.0,<10
pytest-order>=1.3

# Coverage
pytest-cov>=5.0
coverage>=7.4
