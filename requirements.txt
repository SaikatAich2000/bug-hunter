fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.4
psycopg[binary]==3.2.3
python-dotenv==1.0.1
httpx==0.28.1
python-multipart==0.0.20
bcrypt==4.2.1
itsdangerous==2.2.0

# T6: EXIF / metadata stripper for uploaded images. Pillow is lazily
# imported by app/image_strip.py so deploys that skip it (or fail to
# install it) still serve uploads — they just keep the source metadata.
Pillow==12.2.0

# --- Sleuth: Excel export ---
# openpyxl is pure-Python and lightweight (~3 MB), suitable for the
# 1-vCPU / 2 GB target box. It powers the "export to excel" replies.
openpyxl==3.1.5

# --- Web push (Firebase Cloud Messaging) ---
# Backend transport for browser push notifications. Lazily imported by
# app/fcm_transport.py, and only used when WEB_PUSH_ENABLED=true AND a Firebase
# service-account JSON is configured — so a deploy that doesn't use push runs
# fine even if this import is unavailable (the transport just disables itself).
firebase-admin==6.6.0

# --- Sleuth: optional local LLM layer ---
# Uncomment to enable Layer 3 (local llama.cpp inference). Requires a
# GGUF model file at models/sleuth.gguf. See models/README.md for
# recommended models and the privacy/RAM tradeoffs.
# llama-cpp-python==0.3.2
